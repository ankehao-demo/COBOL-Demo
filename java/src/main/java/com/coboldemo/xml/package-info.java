/**
 * XML Serialization — COBOL XML GENERATE demonstrations.
 *
 * Migrated from: xml_generate/xml_generate.cbl
 *
 * Demonstrates the COBOL XML GENERATE command which serializes a record
 * structure to XML. Uses JAXB marshalling with annotations for element naming
 * (@XmlElement), attribute mapping (@XmlAttribute), XML declaration inclusion,
 * and suppression of empty fields (SUPPRESS WHEN SPACES).
 */
package com.coboldemo.xml;
