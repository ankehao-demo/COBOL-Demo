/**
 * Console I/O Programs — COBOL ACCEPT verb demonstrations.
 *
 * Migrated from: accept/accept.cbl, accept/accept-secure.cbl, accept/accept_from.cbl
 *
 * Demonstrates various forms of the COBOL ACCEPT verb including basic input,
 * secure (masked) input, timeout, auto-skip, no-echo, uppercase conversion,
 * and system information retrieval (date, time, user name, environment variables,
 * command-line arguments).
 */
package com.coboldemo.accept;
