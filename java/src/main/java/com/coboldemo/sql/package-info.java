/**
 * Database Integration — COBOL embedded SQL (EXEC SQL) demonstrations.
 *
 * Migrated from: sql/sql_example.cbl
 *
 * Demonstrates COBOL embedded SQL with PostgreSQL including connecting,
 * declaring cursors, fetching records, parameterized queries with LIKE,
 * and error handling via SQLSTATE/SQLCODE. Uses JDBC in Java.
 *
 * Prerequisites: PostgreSQL with create_test_db.sql executed.
 */
package com.coboldemo.sql;
