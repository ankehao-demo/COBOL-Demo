/**
 * String Trimming — COBOL intrinsic FUNCTION TRIM demonstrations.
 *
 * Migrated from: trim/trim.cbl
 *
 * Demonstrates the COBOL FUNCTION TRIM with LEADING, TRAILING, and default
 * (both) options. Java equivalents: String.trim(), stripLeading(), stripTrailing().
 * Also shows how trimmed values behave when moved to fixed-length PIC X fields.
 */
package com.coboldemo.trim;
