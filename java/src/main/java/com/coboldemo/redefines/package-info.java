/**
 * REDEFINES — COBOL REDEFINES clause demonstrations.
 *
 * Migrated from: redifines/redefines.cbl
 *
 * Demonstrates the COBOL REDEFINES clause which allows the same memory area
 * to be interpreted as different data types (union-type memory overlay).
 * Java uses a discriminated union pattern with type fields and separate
 * class members for each interpretation.
 */
package com.coboldemo.redefines;
