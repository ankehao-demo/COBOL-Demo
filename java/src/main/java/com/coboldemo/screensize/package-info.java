/**
 * Screen Size — COBOL terminal dimension retrieval demonstrations.
 *
 * Migrated from: screen_size/get_screen_size.cbl
 *
 * Demonstrates getting the terminal screen size using ACCEPT FROM LINES/COLUMNS
 * and CBL_GET_SCR_SIZE. Uses the Lanterna library for terminal size detection,
 * with a fallback to environment variables.
 */
package com.coboldemo.screensize;
