/**
 * File I/O and Sorting — COBOL SORT and MERGE demonstrations.
 *
 * Migrated from: merge_sort/merge_sort_test.cbl
 *
 * Demonstrates COBOL SORT and MERGE operations on fixed-width record files.
 * Creates test data in two files, merges them sorted by customer ID, then
 * re-sorts the merged file by descending contract ID.
 */
package com.coboldemo.mergesort;
